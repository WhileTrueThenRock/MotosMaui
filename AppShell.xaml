<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiMotos.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MauiMotos.ViewModels"
    xmlns:local="clr-namespace:MauiMotos"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

    <!--<TabBar>
        <Tab Title="PDF Consulting">
            <ShellContent
             Title="Consultas"
             ContentTemplate="{DataTemplate local:FrontPage}"
             Route="FrontPage" />
        </Tab>
        <Tab Title="Home">

        </Tab>
    </TabBar>-->

</Shell>
